# SELECT title from books WHERE title like '%Stories%';

# SELECT title,pages from books  ORDER by  pages DESC LIMIT 0,1;

# SELECT concat(title,' - ',released_year) as summary from books ORDER by released_year DESC limit 0,3;

#  source assinment1.sql; SELECT title,author_lname from books WHERE author_lname like '% %';

# SELECT title,released_year,stock_quantity from books ORDER by stock_quantity  limit 0,4;

#lecect * from books;
# SELECT name, birthdate FROM people;

